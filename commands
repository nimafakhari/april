docker compose  up -d --build     
docker exec -it new-app-1 python -c "import os, redis; r=redis.Redis(host=os.getenv('REDIS_HOST','redis'), port=int(os.getenv('REDIS_PORT',6379))); print(r.ping())"

I just need to run this command docker compose up -d --build

Or I need to delete the previous container first.

docker compose down
docker compose up -d --build